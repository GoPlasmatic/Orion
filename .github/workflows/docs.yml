name: Deploy Documentation

on:
  push:
    branches: [main]
    paths:
      # Not the whole of docs/: openapi.json is regenerated on API changes but
      # is not part of the published book, so it must not redeploy Pages.
      - "docs/src/**"
      - "docs/book.toml"
      - "docs/css/**"
      - "docs/js/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build mdBook
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false

      # Same pinned version as the ci.yml book job — the PR check must
      # exercise the exact mdbook that deploys.
      - name: Install mdBook
        uses: taiki-e/install-action@6a1bd70eaac3c8bdf093356838d7ee09fda951cf # v2
        with:
          tool: mdbook@0.5.2

      - name: Build book
        run: mdbook build docs

      # llms-full.txt: the whole book as one markdown file, in SUMMARY order,
      # served from the site root for LLM/agent consumption (llms.txt is a
      # static file in docs/src and is copied by mdbook build).
      - name: Generate llms-full.txt
        run: |
          {
            echo "# Orion — complete documentation"
            echo "# Generated from the mdBook sources at https://github.com/GoPlasmatic/Orion/tree/main/docs/src"
            grep -oE '\(\./[A-Za-z0-9_./-]+\.md\)' docs/src/SUMMARY.md | tr -d '()' | while read -r p; do
              printf '\n\n---\nsource: %s\n---\n\n' "$p"
              cat "docs/src/${p#./}"
            done
          } > docs/book/llms-full.txt

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: docs/book

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
