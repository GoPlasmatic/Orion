apiVersion: v1
kind: Service
metadata:
  name: {{ include "orion.fullname" . }}
  labels:
    {{- include "orion.labels" . | nindent 4 }}
    # Distinguishes this Service from the devStack postgres/redis Services,
    # which carry the same base labels. The ServiceMonitor selects on it.
    app.kubernetes.io/component: server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if and .Values.metrics.enabled .Values.metrics.service.enabled }}
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "orion.selectorLabels" . | nindent 4 }}
    # Excludes the devStack postgres/redis pods, which share the base
    # selector labels with their own component values.
    app.kubernetes.io/component: server
