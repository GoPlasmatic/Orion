{
  "name": "high severity routes to every channel",
  "workflow": "../packages/notification-routing/workflow.json",
  "input": { "message": "Disk usage at 92%", "severity": "high" },
  "expect": {
    "data.notification.logged": true,
    "data.notification.email_sent": true,
    "data.notification.sms_sent": true
  }
}
